let raioCirculo = 1;

Comprimento = (2*Math.PI*raioCirculo).toFixed(1);

Area = (Math.PI*Math.pow(raioCirculo, 2)).toFixed(1);

console.log(`Como o raio deste circulo é ${raioCirculo}, o comprimento é ${Comprimento} u.m e a área é ${Area} u.m`);

